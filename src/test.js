let exp = "23+22";
var diffWaysToCompute = function(expression) {
    // if(parseInt(expression).toString() == expression ){
    //   let val = parseInt(expression);
    //   return [val];
    // }
return parseInt(expression);

};
console.log(diffWaysToCompute(exp));